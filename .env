# DEV Staging PRD 默认是DEV
ENV=dev
SYSTEM_NAME=resource_service
PROT=3000

# 下面是的配置理论上从ZK获取 然后 Merge 本地部分配置 成为一个 可用正常的使用 的 Config
# 只依赖本地 Config 不行，只依赖远程zk的也不行 ，折中方案就是结合一起用
ZK_HOST_ROOT_PATH=Service_resource
ZK_HOST=localhost:2181